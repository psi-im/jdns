@PACKAGE_INIT@

if(NOT TARGET jdns)
    include("${CMAKE_CURRENT_LIST_DIR}/JDnsTargets.cmake")
endif()

if(TARGET jdns AND NOT TARGET JDns::JDns)
    add_library(JDns::JDns ALIAS jdns)
endif()

set(JDns_LIBRARY jdns)
